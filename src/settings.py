class Settings:
    # Fonts & co
    # TODO: Get fonts from file - use the font file, not system fonts
    # TODO: Maby Singleton
    title_font = 'Pixeled'
    body_font = 'Produkt'

    logo_format = (title_font, 25)
    tab_buttons_format = (title_font, 20)

    large_title_format = (title_font, 30)
    small_title_format = (title_font, 15)
    body_format = (body_font, 15)

    # Colors:
    colors = ['#e53aa3', '#921231', '#f83d32', '#ff851b', '#ffdc00', '#4fcc30', '#1f93ff', '#88d8f1', '#999999', '#000000']

